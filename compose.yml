services:
  drawdb:
    build: .
    image: drawdb-collaborative
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATABASE_PATH: /data/drawdb.sqlite
    volumes:
      - drawdb-data:/data

volumes:
  drawdb-data:
